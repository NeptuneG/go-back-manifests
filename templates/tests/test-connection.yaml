apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "go-back.fullname" . }}-test-connection"
  labels:
    {{- include "go-back.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "go-back.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
